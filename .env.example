# Copy this file to .env and fill in your own values.
# Never commit your real .env file — it's already excluded via .gitignore.

# The contact form uses Netlify Forms and needs no environment variables.
# Submissions are captured automatically when deployed on Netlify; configure
# the email notification (to Rob) under Netlify → Forms → Notifications.
# There are currently no required env vars for this project.
